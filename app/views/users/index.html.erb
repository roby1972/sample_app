<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if @users.any? %>
  <h3>Users (<%= @users.count %>)</h3>

  <%= will_paginate %>

  <ul class="users">
    <%= render @users %>
  </ul>

  <%= will_paginate %>
<% end %>